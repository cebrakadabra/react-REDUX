import React from "react";

export default class Readme extends React.Component {
  render() {
    return (
      <div>
        <h1>Readme</h1>
        <p>This is a reactJS FLUX default setup with react components and of course babel for ES6 usage</p>
      </div>
    );
  }
}
